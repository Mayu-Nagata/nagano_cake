<div class="container mt-4">
  <div class="row">
    <div class="bg-secondary col-md-3">
      <h4 class="text-light">ジャンル編集</h4>
    </div>
  </div>
  <div class="row">
    <diV class="col-md-6">
    <div class="mt-3">
      <%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
        <%= f.label :name, "ジャンル名", class: "mr-4"%>
        <%= f.text_field :name %>
        <%= f.submit "変更を保存", class: "btn btn-success ml-4" %>
      <% end %>
    </div>
  </div>
</div>